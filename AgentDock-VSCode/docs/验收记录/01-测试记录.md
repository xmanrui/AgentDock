# 测试记录

验收日期：2026-07-12

## 环境

- macOS arm64
- Node.js `24.13.0`
- npm `11.6.2`
- TypeScript `5.7.x`
- VS Code Extension Host `1.128.0`（`@vscode/test-electron` 隔离下载）
- VS Code 最低声明版本 `1.95.0`

## 静态检查与单元测试

命令：

```bash
npm run check
npm test -- --reporter=verbose
```

结果：

- TypeScript strict：通过
- ESLint：通过
- 11 个测试文件
- 51 个测试用例
- 覆盖命令渲染、发现、预览、Token/响应时间、额度解析、凭据安全、CLI 探测、持久化、终端事件、流式文本、退出标记和状态机
- V8 覆盖率：Statements 74.33%、Branches 63.50%、Functions 80.56%、Lines 80.73%；Extension Host 和 Webview 交互由集成/视觉测试补充

## Extension Host 集成测试

命令：

```bash
npm run test:integration
```

验证内容：

1. 开发扩展被隔离 VS Code 正确加载和激活。
2. Refresh / Settings 命令已注册。
3. 三家 Provider 的默认 YOLO 模板可从 Configuration API 读取。
4. 创建真实 VS Code Terminal。
5. 临时假 Codex CLI 通过可执行探测。
6. 历史追加 `task_started` 后进入 Working。
7. Shell Integration 输出进入实时 ticker 状态。
8. 历史追加 `task_complete` 后进入 Ready。
9. 假 CLI 退出后 `terminalOpen=false` 且状态回到 Idle。

结果：连续两次返回 `Exit code: 0`。

## Webview 视觉与交互

测试入口：`test/webview/fixture.html`

使用生产构建的 `dist/webview.js`，不是另写的静态仿制页面。

验证视口：

- 默认宽视口 `1280×720`
- 窄侧栏 `480×900`

结果：

- `documentElement.scrollWidth === innerWidth`，无横向溢出。
- 三张卡片的 Open / YOLO / Pin 全部落在容器内。
- Token 和 Avg. Time 均绘制 7 个日数据点。
- Working、Ready、Idle 三种状态显示正常。
- 聊天预览显示 AI 左/用户右、遗漏消息提示和 Provider Logo。
- Provider usage 显示 Token pill、reset pill、两条额度和完整时间。
- Usage popup 与触发 Logo 几何区域不相交。
- 两种弹框圆角裁切正常，无黑色外角。
- 浏览器 console error 数量为 0。

## 真实本地数据基准

当前机器历史规模：

- Codex 历史文件：482
- Claude Code 历史文件：14
- Gemini CLI 历史文件：14

当前 AgentDock 项目发现结果：

- 总会话：33
- Codex：27
- Claude Code：5
- Gemini CLI：1

基准结果：

- 首次发现约 5.3 秒（运行间有磁盘波动；workspace 缓存会先显示旧卡片）
- 33 个会话指标计算约 0.58 秒
- 31 个会话可读取 Token 指标

## 依赖审计

升级 Vitest、Vite 依赖链、esbuild 和 vsce 后：

```text
info 0, low 0, moderate 0, high 0, critical 0, total 0
```

生产依赖与完整开发依赖审计均为 0。

## 打包

最终命令：

```bash
npm run package
```

产物：`dist/agentdock-vscode.vsix`

- 大小：42.34 KB
- 文件数：11
- SHA-256：`685ed454a243de4da95ea885d15f1e694557bc2e14cd88377a57758763065676`
- 不包含 source map、源码、测试、验收文档、node_modules 或 `.gitignore`

隔离安装命令成功，VS Code 列表结果：

```text
xmanrui.agentdock@1.0.0
```
